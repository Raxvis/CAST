# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
# Claude
.claude/settings.local.json

# Local CAST pipeline runs inside this repo (repo root only — the payload's
# skills/cast-init/assets/artifacts/ must stay tracked)
/artifacts/